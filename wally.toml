[package]
name = "st3ameyeggroll/duck-game"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

# Add runtime dependencies here, then run `wally install`.
[dependencies]

# Dev-only dependencies (testing, tooling) go here.
[dev-dependencies]
